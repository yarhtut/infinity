<%= form_for @project, url: {action: "update"}, html: {class: "nifty_form"} do |f| %>
  <%= f.text_field :title %>
  <%= f.text_area :description, size: "60x12" %>
  <%= f.submit "Update" %>
<% end %>

<%= link_to 'Back', projects_path %>
